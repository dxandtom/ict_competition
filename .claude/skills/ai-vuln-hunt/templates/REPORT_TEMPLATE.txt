# Vulnerability Discovery Report

> Generated by the `ai-vuln-hunt` skill. Black-box, evidence-driven. Only PoC-confirmed
> findings appear in Section 3; everything else is quarantined as UNCONFIRMED.

## 1. Scope and method
- **Target:** content-addressed by a source-tree content hash only (see
  `env_manifest.json:target_tree_sha256`, computed over `target_tree_manifest.txt`).
  The target's name and version were never read or used (black-box contract).
- **Method:** SCA (dependency CVE matching) -> SAST (tools + LLM semantic review with the
  sink taxonomy) -> candidate triage/dedup -> multi-auditor cross-validation ->
  DAST/PoC confirmation (Atheris + libFuzzer + property/differential testing) -> scoring.
- **Detected stack:** <python / C++ / Bazel / ... auto-detected>.
- **Surfaces prioritized:** <list the high-risk subtrees you focused on and why>.

## 2. Reproducibility
- Full hash-chained interaction ledger: `ledger.jsonl` (verify with
  `scripts/ledger.sh verify <findings_dir>` — chain intact).
- Environment + exact tool/model versions: `env_manifest.json`.
- All tool raw outputs preserved untouched under `raw/`; every prompt/response is a content-
  addressed blob under `blobs/`.
- Determinism (stated honestly): tool inputs/outputs replay exactly (content-addressed);
  LLM steps run at temperature 0 and are fully logged but are not bit-reproducible (no exposed
  sampling seed). Because findings are PoC-gated, each confirmed finding reproduces with one
  command regardless of which run surfaced it.

## 3. Confirmed findings (ordered by severity score)
| ID | Title | Sink | Severity | CWE | Oracle | Repro |
|----|-------|------|----------|-----|--------|-------|
| VH-0001 | ... | oob_rw | HIGH (8.1) | CWE-787 | ASAN | `findings/VH-0001/run.sh` |

For each finding see `findings/VH-NNNN/` (finding.md + poc + 3 evidence logs + oracle.json).

## 4. Unconfirmed leads (no PoC — reported as leads only, NOT as bugs)
Summarize what was suspected and why a PoC could not be produced (unreachable,
guarded elsewhere, refuted by the red-team panelist). Files under `unconfirmed/`.

## 5. SCA dependency exposure
Outdated/vulnerable third-party dependencies from `sbom/` + normalized `findings.json`.
These are component-level (not host identity). High/medium rows that also yielded a PoC
are promoted into Section 3.

## 6. Black-box compliance statement
- Identity files seen-but-unread are listed in `env_manifest.json:identity_files_seen_but_unread`.
- No prompt, command, or ledger record contains project-name/version/known-CVE reasoning
  about the host target (`blackbox_guard.sh` enforced and logged).
